# Copy this file to server/.env and fill in your own values.

# The backup container uses RCON to flush the world before copying it.
# Generate one with: openssl rand -base64 24
RCON_PASSWORD=change-me

# A restart must never pull a different Minecraft version, so pin it here
# and bump it when you actually want to update.
MC_VERSION=26.2
